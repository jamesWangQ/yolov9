# yolov9/setup.py
from setuptools import setup, find_packages

setup(
    name='yolov9',
    version='0.1.0',  # Use a sensible version number
    packages=find_packages(),
    install_requires=[
    ],
)